% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_objective.R
\name{penalty_sc_total}
\alias{penalty_sc_total}
\title{Total self-containment penalty across all markets (Equation 13, summed)}
\usage{
penalty_sc_total(sc_vec, SC_min, r)
}
\arguments{
\item{sc_vec}{Numeric vector of market self-containment values, typically
\code{\link{build_lma_df}(sol, W, row_W, col_W)$sc}.}

\item{SC_min}{Numeric in (0, 1], the minimum self-containment threshold.}

\item{r}{Integer, steepness exponent.}
}
\value{
Numeric scalar in \code{[0, length(sc_vec)]}.
}
\description{
Sums \code{\link{penalty_sc_single}()} over every market in a partition.
}
\details{
\deqn{P_{SC}(x) = \sum_{M_s \in M} P_{SC}(M_s)}

Since each term lies in \code{[0, 1]}, the total is bounded by the number of
markets and is readable as "how many markets' worth of self-containment the
partition is missing". A total of 2.5 over 40 markets means the shortfall is
equivalent to two and a half markets having no self-containment at all.
}
\examples{
W <- matrix(1, 6, 6); W[1:3, 1:3] <- 30; W[4:6, 4:6] <- 30; diag(W) <- 60
row_W <- rowSums(W); col_W <- colSums(W)

lma <- build_lma_df(c(1, 1, 1, 2, 2, 2), W, row_W, col_W)
penalty_sc_total(lma$sc, SC_min = 0.55, r = 3)      # a good partition: 0

lma_bad <- build_lma_df(c(1, 1, 2, 2, 3, 3), W, row_W, col_W)
penalty_sc_total(lma_bad$sc, SC_min = 0.95, r = 3)

}
\seealso{
\code{\link{penalty_sc_single}()}, \code{\link{penalty_total}()}

Other penalty functions: 
\code{\link{penalty_concentration}()},
\code{\link{penalty_pop_max_single}()},
\code{\link{penalty_pop_max_total}()},
\code{\link{penalty_pop_min_single}()},
\code{\link{penalty_pop_min_total}()},
\code{\link{penalty_sc_single}()},
\code{\link{penalty_total}()}
}
\concept{penalty functions}
